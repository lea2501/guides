# Apply all patches
```shell
# syspatch
```

# Show available patches
```shell
# syspatch -c
```

# Show all installed patches
```shell
# syspatch -l
```

# Revert the most recently installed patch
```shell
# syspatch -r
```
